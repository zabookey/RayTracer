#ifndef SPHERE_COLLISION
#define SPHERE_COLLISION

#include "Sphere.hpp"
#include "Ray.hpp"
#include <cmath>

/* This file has become depracated and replaced with sphere::collision(ray) */

double sphereCollision(Ray r, Sphere s);

#endif
